# <AppName> — Features & Workflow

> Created in Phase 0c — see `references/phase-0-prepare.md`.
> Source of truth for: Phase 4 `semantic` scoring + Phase 5 Description.
> Never invent a feature here; never list a capability the app does not have.
>
> **Language:** This file is **app-wide, not per-locale**. Write it in the
> language you can easily verify against the actual product (English is
> typical). Phase 5 then writes each locale's Description in the locale's
> native language, but every claim it makes must trace back to a bullet
> here. Localised audience names (e.g. "carer" vs "Pfleger") are still
> single-language here; Phase 5 translates them naturally per locale.

## Free features
- <one short capability line>
- <one short capability line>
- …

## Pro features
- <one short capability line>
- <one short capability line>
- …

## Workflow
<2–4 short paragraphs of narrative — how a typical user uses the app,
from first open through daily use. Phase 5 adapts this into the App
Store Description hook in the locale's native language.>

## Audiences
- <audience>
- <audience>
- …
